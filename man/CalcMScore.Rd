% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CalcMScore.R
\name{CalcMScore}
\alias{CalcMScore}
\title{Calculate M-scores using median and MAD}
\usage{
CalcMScore(
  df,
  variables = NULL,
  names_prefix = "M_",
  RetainLabels = TRUE,
  RenameLabels = TRUE,
  center = TRUE,
  scale = TRUE,
  constant = 1.4826
)
}
\arguments{
\item{df}{Data frame with variables to robust-standardize.}

\item{variables}{Character vector of variable names. If \code{NULL}, uses
\code{SciDataReportR::getNumVars(df, Ordinal = FALSE)}.}

\item{names_prefix}{Prefix to prepend to variable names. Default is \code{"M_"}.}

\item{RetainLabels}{Logical; if \code{TRUE} and \code{Hmisc} is available, copy labels.}

\item{RenameLabels}{Logical; if \code{TRUE}, apply the same prefix to labels.}

\item{center}{Logical; if \code{TRUE}, subtract the median.}

\item{scale}{Logical; if \code{TRUE}, divide by the MAD.}

\item{constant}{Numeric scaling constant passed to \code{stats::mad()}. Default is
\code{1.4826}.}
}
\value{
An object of class \code{"MScoreObj"}, a list with:
\itemize{
\item \code{MScores}: data frame of M-score variables only
\item \code{DataWithM}: original \code{df} plus M-score variables
\item \code{Parameters}: data frame with \code{Variable}, \code{N}, \code{Median}, and \code{MAD}
\item \code{Center}: logical flag used
\item \code{Scale}: logical flag used
\item \code{Constant}: MAD scaling constant used
}
}
\description{
Calculate robust standardized scores using the median as the center and
median absolute deviation as the scale. M-scores are useful for skewed,
heavy-tailed, or outlier-prone variables where standard z-scores may be
overly influenced by the mean and standard deviation.
}
\details{
An M-score is calculated as:

\code{(x - median(x)) / MAD(x)}

By default, \code{stats::mad()} uses a scaling constant of \code{1.4826}, which makes
MAD approximately comparable to the standard deviation when data are normally
distributed.
}
