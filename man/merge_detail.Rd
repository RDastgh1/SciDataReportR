% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merge_detail.R
\name{merge_detail}
\alias{merge_detail}
\title{Print a plain-text detail report for one safe_merge result}
\usage{
merge_detail(m, TopN = 10)
}
\arguments{
\item{m}{A list returned by \code{\link[=safe_merge]{safe_merge()}} (must contain a \code{validation}
element from \code{\link[=ValidateMerge]{ValidateMerge()}} and a \code{log} tibble).}

\item{TopN}{Integer. Maximum number of rows shown for the left-only and
right-only unmatched-key previews. Default is \code{10}.}
}
\value{
\code{m}, invisibly. Called for its printed output.
}
\description{
Print static \code{knitr::kable()} tables (no plots, no htmlwidgets) for the key
diagnostic sections of a \code{\link[=safe_merge]{safe_merge()}} result: the validation checks,
unmatched key combinations on each side, overlapping non-key variables, and
suspicious duplicate-variable conflicts. Sections with nothing to show are
skipped entirely.
}
\details{
In R Markdown or Quarto, use a chunk with \code{results = "asis"} so the kable
markdown renders as tables.
}
\examples{
baseline <- data.frame(id = 1:4, age = c(50, 61, 45, 58))
labs <- data.frame(id = c(1, 2, 4), glucose = c(90, 110, 100))
m <- safe_merge(baseline, labs, by = "id", name = "Baseline + labs")
merge_detail(m)

}
\seealso{
\code{\link[=safe_merge]{safe_merge()}}, \code{\link[=merge_summary_table]{merge_summary_table()}}, \code{\link[=ExploreMergeValidation]{ExploreMergeValidation()}}
}
