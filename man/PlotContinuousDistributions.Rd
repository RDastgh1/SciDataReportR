% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PlotContinuousDistributions.R
\name{PlotContinuousDistributions}
\alias{PlotContinuousDistributions}
\title{Plot Continuous Distributions}
\usage{
PlotContinuousDistributions(
  data,
  variables = NULL,
  Fill = NULL,
  Relabel = TRUE,
  FacetLabelStyle = c("both", "label_only", "variable_only", "auto"),
  ncol = 3,
  Ordinal = TRUE,
  DataFrame = lifecycle::deprecated(),
  Variables = lifecycle::deprecated()
)
}
\arguments{
\item{data}{A data frame containing the variables to be plotted.}

\item{variables}{Character vector of column names to plot.}

\item{Fill}{Optional column name for grouping.}

\item{Relabel}{Logical; use variable labels when available.}

\item{FacetLabelStyle}{One of "both", "label_only", "variable_only", "auto".}

\item{ncol}{Number of columns in the facet grid.}

\item{Ordinal}{Logical; include labelled-ordinal variables as numeric.}

\item{DataFrame}{\strong{Deprecated} (since 19.15.0). Use \code{data} instead.}

\item{Variables}{\strong{Deprecated} (since 19.15.0). Use \code{variables} instead.}
}
\value{
A ggplot object.
}
\description{
Creates rain-cloud plots (half-violin + box/median + scatter) for one or
more continuous variables, with optional group-wise colouring.
}
\examples{
data(SampleData)
data(SampleVariableTypes)

Labelled <- RevalueData(SampleData, SampleVariableTypes)$RevaluedData

PlotContinuousDistributions(
  Labelled,
  variables = c("age", "AXL", "Adiponectin")
)
}
