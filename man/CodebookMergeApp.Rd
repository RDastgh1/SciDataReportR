% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MergeCodebooks.R
\name{CodebookMergeApp}
\alias{CodebookMergeApp}
\title{Interactive codebook harmonization dashboard}
\usage{
CodebookMergeApp(
  codebooks,
  VariableCol = "Variable",
  auto_type_mapping = TRUE,
  ignore_columns = NULL
)
}
\arguments{
\item{codebooks}{Named list of codebook data frames.}

\item{VariableCol}{Name of variable identifier column.}

\item{auto_type_mapping}{Logical; normalize common type synonyms.}

\item{ignore_columns}{Optional metadata columns to ignore.}
}
\value{
Launches a Shiny app.
}
\description{
Launch a Shiny dashboard for reviewing and harmonizing multiple
codebooks before deterministic merging with MergeCodebooks().
}
\section{Dashboard walkthrough}{

Because this function launches an interactive Shiny dashboard, the reference
page uses static screenshots. Regenerate them with
\code{data-raw/CodebookMergeApp-screenshot.R}, which drives the app with
\pkg{shinytest2} and writes to \verb{man/figures/}.

\subsection{1. Overview}{
Start on the Overview tab. Variable Presence shows whether each variable is
available in every source codebook; Structure Comparison shows the columns
supplied by each source. Use these tables to distinguish expected
cohort-specific variables from schema differences that need review.
}

\if{html}{\figure{CodebookMergeApp.png}{options: width="800" alt="Overview tab showing variable presence and structure comparison"}}

\subsection{2. Harmonization}{
The Harmonization tab lists each conflicting variable-column pair and its
source values. Select a conflict, choose one observed value or enter a custom
value, then select Save Resolution. Saved choices appear below the resolution
controls and are used when rules are generated; they do not alter the source
codebooks.
}

\if{html}{\figure{CodebookMergeApp-harmonization.png}{options: width="800" alt="Harmonization tab showing the conflict browser and resolution controls"}}

\subsection{3. Export and merge}{
On Export, select Generate Rules to create reproducible \code{MergeRules} code and
inspect the merged-codebook preview. Copy the rules or download the preview,
then pass the generated object to \code{\link[=MergeCodebooks]{MergeCodebooks()}} through its \code{Rules}
argument to repeat the reviewed merge deterministically.
}

\if{html}{\figure{CodebookMergeApp-export.png}{options: width="800" alt="Export tab showing generated merge rules and merged codebook preview"}}
}

\examples{
\dontrun{
data(SampleVariableTypes)

# Two overlapping codebooks to harmonize before a deterministic merge
cb_a <- SampleVariableTypes[1:12, c("Variable", "Label", "Type")]
cb_b <- cb_a[-(1:2), ]
cb_b$Type[cb_b$Type == "Double"] <- "numeric"

# Launch the interactive harmonization dashboard
CodebookMergeApp(
  codebooks = list(CohortA = cb_a, CohortB = cb_b),
  VariableCol = "Variable"
)
}
}
