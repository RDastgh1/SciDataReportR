% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PrepNumericData.R
\name{PrepNumericData}
\alias{PrepNumericData}
\title{Prepare numeric data safely for analysis}
\usage{
PrepNumericData(df, variables = names(df))
}
\arguments{
\item{df}{A data frame.}

\item{variables}{Character vector of variable names to process.
Defaults to all columns.}
}
\value{
A data frame with selected variables converted to numeric and
non-finite values replaced with \code{NA}.
}
\description{
Safely coerces selected variables in a data frame to numeric format while
preserving column names and replacing non-finite values (\code{Inf}, \code{-Inf},
\code{NaN}) with \code{NA}.
}
\details{
This function is useful for preparing real-world biomedical, omics, and
survey datasets for downstream statistical analyses such as correlations,
PCA, clustering, regression, and factor analysis.

Character and factor variables are converted using
\code{as.numeric(as.character(x))} to avoid incorrect factor level coercion.
}
\examples{
df <- data.frame(
  x = c("1", "2", "3"),
  y = c(1, 2, Inf),
  z = factor(c("4", "5", "6"))
)

PrepNumericData(df)

}
