% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PlotClusterBoxplot.R
\name{PlotClusterBoxplot}
\alias{PlotClusterBoxplot}
\title{Plot cluster boxplots by variable}
\usage{
PlotClusterBoxplot(
  Data,
  ClusterVar,
  Variables,
  Codebook = NULL,
  Scale = FALSE,
  ScoreType = c("auto", "z", "t", "raw"),
  ReferenceLines = c("auto", "z", "t", "none"),
  ClusterLabel = c("n_percent", "n", "none"),
  Relabel = TRUE,
  FillTitle = "Test",
  Palette = NULL,
  YLabel = NULL,
  BoxplotWidth = 0.75,
  OutlierSize = 0.8,
  BaseSize = 14
)
}
\arguments{
\item{Data}{A data frame.}

\item{ClusterVar}{Character string naming the cluster/grouping variable.}

\item{Variables}{Character vector of variable names to plot.}

\item{Codebook}{Optional codebook data frame with columns \code{Variable} and
\code{Label}.}

\item{Scale}{Logical. If \code{TRUE}, variables are z-scored across all
participants before plotting. Default is \code{FALSE}.}

\item{ScoreType}{Character. One of \code{"auto"}, \code{"z"}, \code{"t"}, or \code{"raw"}.
Used for axis labeling and reference-line defaults.}

\item{ReferenceLines}{Character. One of \code{"auto"}, \code{"z"}, \code{"t"}, or
\code{"none"}.

If \code{"auto"}, z-score reference lines are added only when \code{Scale = TRUE}.

If \code{"z"}, lines are added at \code{-1}, \code{-0.5}, \code{0}, \code{0.5}, and \code{1}.

If \code{"t"}, lines are added at \code{40}, \code{45}, \code{50}, \code{55}, and \code{60}.

If \code{"none"}, no reference lines are added so users can customize
overlays manually using additional ggplot layers.}

\item{ClusterLabel}{Character. One of \code{"n_percent"}, \code{"n"}, or \code{"none"}.

\code{"n_percent"} adds cluster sample size and percent to the x-axis label.

\code{"n"} adds only sample size.

\code{"none"} uses only the cluster name/value.}

\item{Relabel}{Logical. If \code{TRUE}, variable labels are used when available.
Labels are pulled first from \code{Codebook}, then from variable label
attributes. Default is \code{TRUE}.}

\item{FillTitle}{Character string used as the fill legend title. Default is
\code{"Test"}.}

\item{Palette}{Optional character vector of colors used for the fill scale.
If \code{NULL}, a stable 20-color default palette is used. If more than 20
variables are plotted, colors are interpolated from the default palette so
the function does not fail.}

\item{YLabel}{Optional y-axis label. If \code{NULL}, an appropriate label is
chosen automatically from \code{Scale} and \code{ScoreType}.}

\item{BoxplotWidth}{Numeric width passed to
\code{ggplot2::geom_boxplot()}. Default is \code{0.75}.}

\item{OutlierSize}{Numeric size of outlier points. Default is \code{0.8}.}

\item{BaseSize}{Base font size for the plot theme. Default is \code{14}.}
}
\value{
A \code{ggplot} object.
}
\description{
Create grouped cluster boxplots where clusters are shown on the x-axis and
selected variables are shown as filled boxplots within each cluster. This is
useful for visualizing cognitive, clinical, biomarker, or domain profiles
across discovered clusters or subgroup solutions.
}
\details{
Variables can optionally be z-scored across all participants before plotting.
Reference lines can be automatically added for z-score or T-score style
interpretation, or omitted entirely for custom overlays. By default,
reference lines are only added automatically when \code{Scale = TRUE}.

Variable labels are used by default when available from a codebook or from
variable label attributes.
}
\examples{
PlotClusterBoxplot(
  Data = mtcars,
  ClusterVar = "cyl",
  Variables = c("mpg", "disp", "hp"),
  Scale = TRUE,
  ReferenceLines = "z"
)

PlotClusterBoxplot(
  Data = mtcars,
  ClusterVar = "cyl",
  Variables = c("mpg", "disp", "hp"),
  ClusterLabel = "n"
)

}
