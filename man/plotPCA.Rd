% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotPCA.R
\name{plotPCA}
\alias{plotPCA}
\title{Plot PCA scores}
\usage{
plotPCA(
  PCAObj,
  Var = NULL,
  t = "NULL",
  HoverVar = NULL,
  HoverVars = NULL,
  Components = NULL,
  Mode = c("auto", "3D", "2D"),
  ColorType = c("auto", "factor", "continuous"),
  Relabel = TRUE,
  Title = NULL
)
}
\arguments{
\item{PCAObj}{A PCA object returned by \code{CreatePCAObject()}. Must contain
\code{Scores} and \code{CombinedData}.}

\item{Var}{Optional character string naming a variable in
\code{PCAObj$CombinedData} used to color points. Default is \code{NULL}.}

\item{t}{Deprecated compatibility argument for color type. Use \code{ColorType}
instead. If set to \code{"Factor"}, \code{Var} is treated as categorical. Any other
value treats \code{Var} as continuous. Default is \code{"NULL"}.}

\item{HoverVar}{Optional character string naming one variable in
\code{PCAObj$CombinedData} to display in hover text. Retained for backward
compatibility. Prefer \code{HoverVars} for new code. Default is \code{NULL}.}

\item{HoverVars}{Optional character vector naming one or more variables in
\code{PCAObj$CombinedData} to display in hover text. If \code{NULL}, row number is
shown. Default is \code{NULL}.}

\item{Components}{Optional character or numeric vector specifying which score
columns to plot. Supply two components for 2D or three components for 3D.
If \code{NULL}, the first three score columns are used.}

\item{Mode}{Character. Either \code{"auto"}, \code{"3D"}, or \code{"2D"}. If \code{"auto"},
the plot dimension is inferred from \code{Components}. If \code{Components = NULL},
\code{"auto"} defaults to 3D. Default is \code{"auto"}.}

\item{ColorType}{Character. Either \code{"auto"}, \code{"factor"}, or \code{"continuous"}.
\code{"auto"} treats character, factor, logical, and labelled variables as
categorical and numeric variables as continuous. Default is \code{"auto"}.}

\item{Relabel}{Logical. If \code{TRUE}, labels attached to hover variables are
used in hover text when available. If \code{FALSE}, raw variable names are used.
Default is \code{TRUE}.}

\item{Title}{Optional plot title. Default is \code{NULL}, which produces no title.}
}
\value{
A Plotly htmlwidget.
}
\description{
Create an interactive 2D or 3D Plotly scatter plot from a PCA object created
by \code{CreatePCAObject()}.
}
\details{
By default, the function plots the first three score columns in
\code{PCAObj$Scores} using a 3D plot. These are not assumed to be named \code{RC1},
\code{RC2}, and \code{RC3}; the function uses the current column order in
\code{PCAObj$Scores}. Users may manually specify which components to plot using
\code{Components}.

If \code{Mode = "auto"}, the function chooses 2D when two components are supplied
and 3D when three components are supplied. If \code{Components = NULL}, it defaults
to the first three score columns and creates a 3D plot.

The function can optionally color points by a variable in
\code{PCAObj$CombinedData} and customize hover text using one or more variables.
When \code{Relabel = TRUE}, labels attached to hover variables are used in the
hover display when available.
}
\examples{
\dontrun{
plotPCA(PCAObj)

plotPCA(
  PCAObj,
  Components = c("RC1", "RC2"),
  Var = "SITE",
  ColorType = "factor"
)

plotPCA(
  PCAObj,
  Components = c("RC2", "RC1", "RC4"),
  Mode = "3D"
)

plotPCA(
  PCAObj,
  Components = c("RC1", "RC2"),
  HoverVars = c("SubjectID", "SITE", "Visit")
)

plotPCA(
  PCAObj,
  Components = c("RC1", "RC2"),
  HoverVars = c("SubjectID", "SITE", "Visit"),
  Relabel = FALSE
)
}

}
