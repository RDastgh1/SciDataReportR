% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PlotAnovaRelationshipsMatrix.R
\name{PlotAnovaRelationshipsMatrix}
\alias{PlotAnovaRelationshipsMatrix}
\title{Plot ANOVA Relationships Matrix}
\usage{
PlotAnovaRelationshipsMatrix(
  data,
  CatVars,
  ContVars,
  covariates = NULL,
  Relabel = TRUE,
  Parametric = TRUE,
  Ordinal = FALSE,
  min_n = 4,
  eps = 1e-08,
  Data = lifecycle::deprecated(),
  fdr_scope = c("matrix", "per_outcome"),
  Covariates = lifecycle::deprecated()
)
}
\arguments{
\item{data}{The data frame containing the variables of interest.}

\item{CatVars}{Character vector of categorical variable names.}

\item{ContVars}{Character vector of continuous variable names.}

\item{covariates}{Optional character vector of covariate names for ANCOVA analysis.}

\item{Relabel}{Logical indicating whether to relabel variables with their labels (default is TRUE).}

\item{Parametric}{Logical indicating whether to use parametric (ANOVA) or non-parametric (Kruskal-Wallis) tests (default is TRUE).}

\item{Ordinal}{Logical, indicating whether ordinal variables should be considered.}

\item{min_n}{Minimum number of complete observations required for a tested relationship.}

\item{eps}{Small positive value used to avoid zero-size plotting artifacts.}

\item{Data}{\strong{Deprecated} (since 19.15.0). Use \code{data} instead.}

\item{fdr_scope}{Either \code{"matrix"} (default) or \code{"per_outcome"}, passed to
\code{\link[=ApplyFDRCorrection]{ApplyFDRCorrection()}}. \code{"matrix"} corrects across all p-values at once
(historical behavior). \code{"per_outcome"} corrects separately within each
outcome: outcomes are the continuous variables (\code{ContVars}).}

\item{Covariates}{\strong{Deprecated} (since 19.15.0). Use \code{covariates} instead.}
}
\value{
A list containing three ggplot objects: p (scatter plot without multiple comparison correction), p_FDR (scatter plot with FDR correction), and pvaltable (data frame of p-values and significance).
}
\description{
This function plots the relationship between continuous and categorical variables
using ANOVA or Kruskal-Wallis tests. It generates a "heatmap" with points
colored and shaped based on statistical significance and effect size. When
generalized eta-squared is unavailable, raw p-values are colored with
\code{\link[=scale_color_pvalue]{scale_color_pvalue()}}; the FDR-corrected plot uses adjusted p-values.
}
\examples{
data(SampleData)
data(SampleVariableTypes)

# Attach labels and factor levels for readable axes
Labelled <- RevalueData(SampleData, SampleVariableTypes)$RevaluedData

result <- PlotAnovaRelationshipsMatrix(
  Labelled,
  CatVars = c("Diagnosis", "sex", "Genotype"),
  ContVars = c("age", "ACE_CD143_Angiotensin_Converti",
               "ACTH_Adrenocorticotropic_Hormon", "AXL", "Adiponectin",
               "Alpha_1_Antichymotrypsin", "Alpha_1_Antitrypsin",
               "Alpha_1_Microglobulin", "Alpha_2_Macroglobulin",
               "Apolipoprotein_A1")
)

result$Unadjusted$plot
}
