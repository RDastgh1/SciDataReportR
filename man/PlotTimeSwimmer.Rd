% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PlotTimeSwimmer.R
\name{PlotTimeSwimmer}
\alias{PlotTimeSwimmer}
\title{Plot longitudinal swimmer timelines}
\usage{
PlotTimeSwimmer(
  Data,
  ID,
  Time,
  State = NULL,
  Event = NULL,
  EventType = NULL,
  TimeScale = c("from_first", "observed", "from_event"),
  EventReference = NULL,
  StateInterval = c("forward", "point"),
  Format = c("state_path", "visit_points", "event_rug", "minimal"),
  SortBy = c("duration", "last_time", "first_time", "state", "id"),
  TimeUnit = c("auto", "days", "weeks", "months", "years", "visits"),
  Relabel = TRUE,
  Codebook = NULL,
  LineWidth = 5,
  PointSize = 2.5,
  Alpha = 0.9,
  BaseSize = 13
)
}
\arguments{
\item{Data}{A data frame in long format with one row per visit or observation.}

\item{ID}{Character string naming the participant ID column.}

\item{Time}{Character string naming the time variable.}

\item{State}{Optional character string naming a state/group variable used for
coloring timelines or visit points.}

\item{Event}{Optional character string naming a logical or binary event
indicator variable.}

\item{EventType}{Optional character string naming an event category variable.
Used for event point shapes/colors.}

\item{TimeScale}{Character. One of \code{"from_first"}, \code{"observed"}, or
\code{"from_event"}.

\code{"observed"} uses raw observed time values.

\code{"from_first"} normalizes each subject relative to their first observed
timepoint.

\code{"from_event"} normalizes each subject relative to the first occurrence of
\code{EventReference}.}

\item{EventReference}{Optional event value used when
\code{TimeScale = "from_event"}.}

\item{StateInterval}{Character. One of \code{"forward"} or \code{"point"}.

\code{"forward"} extends the current state forward until the next visit.

\code{"point"} only colors visit points without extending intervals.}

\item{Format}{Character. One of \code{"state_path"}, \code{"visit_points"},
\code{"event_rug"}, or \code{"minimal"}.}

\item{SortBy}{Character. One of \code{"duration"}, \code{"last_time"},
\code{"first_time"}, \code{"state"}, or \code{"id"}.}

\item{TimeUnit}{Character. One of \code{"auto"}, \code{"days"}, \code{"weeks"},
\code{"months"}, \code{"years"}, or \code{"visits"}.}

\item{Relabel}{Logical. If \code{TRUE}, use labels from \code{Codebook} or variable
attributes when available.}

\item{Codebook}{Optional codebook data frame with columns \code{Variable} and
\code{Label}.}

\item{LineWidth}{Numeric line width for swimmer segments. Default is \code{5}.}

\item{PointSize}{Numeric point size for visit/event points. Default is \code{2.5}.}

\item{Alpha}{Numeric alpha transparency for swimmer segments. Default is
\code{0.9}.}

\item{BaseSize}{Base font size for the plot theme. Default is \code{13}.}
}
\value{
A \code{ggplot} object.
}
\description{
Create swimmer-style longitudinal timeline plots from long-format visit data.
Each row in the input data represents a subject visit or observation. The
function can visualize longitudinal state trajectories, visit timing, and
events over time.
}
\details{
Time can be represented using dates or numeric visit values. Timelines can
optionally be normalized relative to each participant's first visit or the
first occurrence of a specified event.

States can either be displayed as continuous intervals extending forward
until the next visit (\code{StateInterval = "forward"}) or shown only as visit
points (\code{StateInterval = "point"}).
}
\examples{
df <- tibble::tibble(
  ID = c(1,1,1,2,2,2),
  Visit = c(0,6,12,0,6,12),
  Cluster = c("A","A","B","B","B","C")
)

PlotTimeSwimmer(
  Data = df,
  ID = "ID",
  Time = "Visit",
  State = "Cluster"
)

}
