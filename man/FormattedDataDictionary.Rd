% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FormattedDataDictionary.R
\name{FormattedDataDictionary}
\alias{FormattedDataDictionary}
\title{Create a formatted data dictionary table}
\usage{
FormattedDataDictionary(
  data,
  digits = 2,
  DataFrame = lifecycle::deprecated(),
  numdecimals = lifecycle::deprecated()
)
}
\arguments{
\item{data}{The data frame for which the data dictionary is to be created.}

\item{digits}{Number of decimals to display for numeric variables (default: 2).}

\item{DataFrame}{\strong{Deprecated} (since 19.15.0). Use \code{data} instead.}

\item{numdecimals}{\strong{Deprecated} (since 19.15.0). Use \code{digits} instead.}
}
\value{
A formatted data dictionary table (gt object).
}
\description{
This function generates a formatted data dictionary table using the specified data frame.
The table includes variable names, labels, types, and additional formatting based on variable types.
}
\details{
This function requires the \code{gt} and \code{codebook} packages. If either
is not installed, the function will return an error.
}
\examples{
data(SampleData)
data(SampleVariableTypes)

# Attach variable labels and factor levels first so the dictionary shows
# human-readable labels and correct variable types.
Labelled <- RevalueData(SampleData, SampleVariableTypes)$RevaluedData

# Formatted data dictionary for a subset of variables
FormattedDataDictionary(
  Labelled[, c("Diagnosis", "age", "sex", "Genotype", "AXL", "Adiponectin")]
)

}
