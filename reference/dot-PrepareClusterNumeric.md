# Reusable numeric clustering models

Fit projectable Mclust or K-means models. These functions keep training
scaling parameters and assign new observations without refitting.

## Usage

``` r
.PrepareClusterNumeric(data, variables, Scaling)
```
