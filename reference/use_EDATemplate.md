# Use the EDATemplate Quarto Template

Copies the EDATemplate template to the working directory.

Copies the EDATemplate template to the working directory.

## Usage

``` r
use_EDATemplate(filename = "Scripts/script_GetStarted.R")

use_EDATemplate(filename = "Scripts/script_GetStarted.R")
```

## Arguments

- filename:

  The name to save the Quarto file as (default: "EDA_Report.qmd").
