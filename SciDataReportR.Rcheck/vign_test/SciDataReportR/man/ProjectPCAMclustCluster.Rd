% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ClusteringPipelines.R
\name{ProjectPCAMclustCluster}
\alias{ProjectPCAMclustCluster}
\title{Project data onto a PCA + Mclust model}
\usage{
ProjectPCAMclustCluster(object, new_df, ClusterName = object$ClusterName)
}
\arguments{
\item{object}{A \code{CreateMclustClusterModel()} result.}

\item{new_df}{New observations.}

\item{ClusterName}{Optional output cluster column name.}
}
\value{
A full-length projection through the frozen scaling, PCA, and
Mclust layers, with posterior membership in \code{ProbFit} and the projection
triage in \code{ProjectionFit}.
}
\description{
Project data onto a PCA + Mclust model
}
\examples{
\donttest{
data("SimulatedPhenotypeData")
df_Training <- dplyr::filter(SimulatedPhenotypeData, .data$Cohort == "Training")
df_Projection <- dplyr::filter(SimulatedPhenotypeData, .data$Cohort == "Projection")
model <- CreatePCAMclustClusterModel(
  df_Training, paste0("Var", 1:12), method = "finalize",
  final_k = 4, final_model = "EEI", stability_resamples = 2
)
projected <- ProjectPCAMclustCluster(model, df_Projection)
head(projected$ProbFit$individual)
projected$ProjectionFit$overall_fit_summary
projected$ProjectionFit$plots$projection_fit_class_bar
}
}
