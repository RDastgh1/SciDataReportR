% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merge_summary_table.R
\name{merge_summary_table}
\alias{merge_summary_table}
\title{Combine safe_merge logs into a single summary table}
\usage{
merge_summary_table(merge_log, flagged_only = FALSE)
}
\arguments{
\item{merge_log}{A list of \verb{$log} tibbles from \code{\link[=safe_merge]{safe_merge()}} results.
Full \code{\link[=safe_merge]{safe_merge()}} result objects are also accepted (the \verb{$log} element
is extracted automatically), as is a single log tibble.}

\item{flagged_only}{Logical. If \code{TRUE}, only rows with
\code{Status != "PASS"} are returned. Default is \code{FALSE}.}
}
\value{
A tibble with one row per merge and the same columns as a
\code{\link[=safe_merge]{safe_merge()}} \verb{$log} tibble.
}
\description{
Bind the one-row \verb{$log} tibbles produced by \code{\link[=safe_merge]{safe_merge()}} into a single
table, one row per merge, optionally filtered to merges that did not pass
cleanly. Useful as an end-of-pipeline rollup after a sequence of merges.
}
\examples{
baseline <- data.frame(id = 1:4, age = c(50, 61, 45, 58))
labs <- data.frame(id = c(1, 2, 4), glucose = c(90, 110, 100))
vitals <- data.frame(id = 1:4, sbp = c(120, 135, 118, 141))
m1 <- safe_merge(baseline, labs, by = "id", name = "Baseline + labs")
m2 <- safe_merge(m1$data, vitals, by = "id", name = "+ vitals")
merge_summary_table(list(m1$log, m2$log))

}
\seealso{
\code{\link[=safe_merge]{safe_merge()}}
}
