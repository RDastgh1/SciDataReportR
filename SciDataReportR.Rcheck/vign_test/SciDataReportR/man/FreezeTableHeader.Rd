% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FreezeTableHeader.R
\name{FreezeTableHeader}
\alias{FreezeTableHeader}
\title{Freeze the header row of a long table when scrolling}
\usage{
FreezeTableHeader(
  x,
  height = NULL,
  width = NULL,
  header_background = "white",
  bootstrap_options = c("striped", "hover", "condensed"),
  full_width = FALSE,
  font_size = NULL,
  ...
)
}
\arguments{
\item{x}{A \code{gtsummary} object (such as the output of \code{\link[=MakeComparisonTable]{MakeComparisonTable()}}),
a data frame or tibble, or an existing \pkg{kableExtra} / \code{knitr_kable}
object.}

\item{height}{Optional CSS height for a scroll box, for example \code{"400px"} or
\code{"60vh"}. When supplied, the table scrolls within a box of this height with
the header frozen. When \code{NULL} (default), the header sticks during
page-level scrolling instead.}

\item{width}{Optional CSS width for the scroll box, for example \code{"100\%"}.
Only used when \code{height} is supplied.}

\item{header_background}{Background color for the frozen header row. Defaults
to \code{"white"} so the header cleanly covers rows scrolling underneath it.}

\item{bootstrap_options}{Character vector of \pkg{kableExtra} bootstrap
styling options passed to \code{kableExtra::kable_styling()}. Defaults to
\code{c("striped", "hover", "condensed")}.}

\item{full_width}{Logical; passed to \code{kableExtra::kable_styling()}. Default
\code{FALSE}.}

\item{font_size}{Optional numeric font size passed to
\code{kableExtra::kable_styling()}.}

\item{...}{Additional arguments passed to \code{kableExtra::kable_styling()}.}
}
\value{
A \pkg{kableExtra} HTML table object with a frozen header, suitable
for printing in a Quarto or R Markdown chunk.
}
\description{
Render a table with a "sticky" header row that stays visible while the reader
scrolls, so the column meanings are never lost in a long table. This is handy
for the tall tables produced by \code{\link[=MakeComparisonTable]{MakeComparisonTable()}} and other
\code{gtsummary} helpers when they are knit into HTML Quarto or R Markdown
documents.
}
\details{
The table is rendered through \pkg{kableExtra} with
\code{kable_styling(fixed_thead = ...)}, which pins the header row using CSS
\code{position: sticky}. Two scrolling modes are supported:
\itemize{
\item With \code{height = NULL} (the default) the header sticks to the top of the
viewport as the whole page scrolls past the table.
\item With a \code{height} (for example \code{"400px"}) the table is placed in its own
scroll box of that height and the header stays frozen at the top of the box.
}

Sticky headers only take effect in HTML output. In PDF or Word output the
table renders normally without a frozen header.
}
\examples{
\donttest{
data(SampleData)
data(SampleVariableTypes)

Labelled <- RevalueData(SampleData, SampleVariableTypes)$RevaluedData

# Repeat the full comparison table to make scrolling and the frozen header
# obvious on the reference page.
tbl_Base <- MakeComparisonTable(
  data = Labelled,
  group_var = "Diagnosis",
  variables = setdiff(names(Labelled), "Diagnosis")
)

tbl_All <- dplyr::bind_rows(rep(list(tbl_Base$table_body), 8))

# Inside a fixed-height vertical scroll box
htmltools::browsable(htmltools::HTML(as.character(
  FreezeTableHeader(tbl_All, height = "450px", width = "100\%")
)))
}

}
\seealso{
\code{\link[=MakeComparisonTable]{MakeComparisonTable()}}
}
