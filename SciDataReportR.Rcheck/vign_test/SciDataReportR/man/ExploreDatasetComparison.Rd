% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ExploreDatasetComparison.R
\name{ExploreDatasetComparison}
\alias{ExploreDatasetComparison}
\title{Explore dataset comparison results interactively}
\usage{
ExploreDatasetComparison(
  CompareObj,
  Title = "Dataset comparison explorer",
  TopN = 10
)
}
\arguments{
\item{CompareObj}{A list returned by \code{CompareDatasets()}.}

\item{Title}{Character title shown at the top of the dashboard. Default is
\code{"Dataset comparison explorer"}.}

\item{TopN}{Integer number of example variables or records to show in
previews and expanded sections. Default is \code{10}.}
}
\value{
An \code{htmltools::tagList()} object containing an interactive dashboard.
}
\description{
Create an interactive HTML dashboard from a \code{CompareDatasets()} result object.
This function is designed for data review and quality-control workflows. It
displays high-level summary cards, a traffic-light checks table, and an
expandable variable-change explorer that shows side-by-side old and new
values for modified cells.
}
\details{
This function is intended for interactive review rather than publication
tables. It returns an HTML object that can be rendered in the RStudio Viewer,
Quarto, R Markdown, Shiny, or standalone HTML. The reference page uses a
static preview because pkgdown cannot host the live dashboard.
}
\section{Dashboard preview}{

\if{html}{\figure{ExploreDatasetComparison.png}{options: width="1200" alt="Dataset comparison dashboard showing summary cards, validation checks, and variable-change explorer"}}
}

\examples{
data(SampleData)

old_data <- cbind(id = seq_len(nrow(SampleData)), SampleData)
new_data <- old_data[-c(1, 2), ]
new_data$MMP7[1:12] <- new_data$MMP7[1:12] * 1.15
new_data$tau[20:35] <- new_data$tau[20:35] + 5
new_data$QualityReview <- ifelse(seq_len(nrow(new_data)) \%\% 3 == 0, "Review", "Pass")
new_data$Smoker <- NULL
new_data <- rbind(
  new_data,
  transform(new_data[1, ], id = max(old_data$id) + 1, QualityReview = "New record")
)

comparison <- CompareDatasets(old_data, new_data, keys = "id")

# Render the dashboard in an HTML report, Shiny app, Viewer, or HTML file
dashboard <- ExploreDatasetComparison(comparison, TopN = 8)
}
