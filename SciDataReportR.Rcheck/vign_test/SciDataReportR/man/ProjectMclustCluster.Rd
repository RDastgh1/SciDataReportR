% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ClusteringPipelines.R
\name{ProjectMclustCluster}
\alias{ProjectMclustCluster}
\title{Project data onto a Mclust cluster model}
\usage{
ProjectMclustCluster(object, new_df, ClusterName = object$ClusterName)
}
\arguments{
\item{object}{A \code{CreateMclustClusterModel()} result.}

\item{new_df}{New observations.}

\item{ClusterName}{Optional output cluster column name.}
}
\value{
A full-length projection object. \code{ProbFit$individual} holds posterior
membership, uncertainty, and distance to the assigned component;
\code{ProjectionFit} triages every projected case against the frozen training
distance reference and holds the projected distance triad, the
training-versus-projected occupancy comparison, and the fit-class bar;
\code{ProbFit$plots} holds posterior-confidence figures.
}
\description{
Project data onto a Mclust cluster model
}
\examples{
\donttest{
data("SimulatedPhenotypeData")
df_Training <- dplyr::filter(SimulatedPhenotypeData, .data$Cohort == "Training")
df_Projection <- dplyr::filter(SimulatedPhenotypeData, .data$Cohort == "Projection")
vars_Numeric <- paste0("Var", 1:12)
model <- CreateMclustClusterModel(
  df_Training, vars_Numeric, method = "finalize", final_k = 4,
  final_model = "EEI", stability_resamples = 2
)
projected <- ProjectMclustCluster(model, df_Projection)
head(projected$ProbFit$individual)
projected$ProjectionFit$overall_fit_summary
projected$ProjectionFit$plots$projection_fit_class_bar
projected$ProjectionFit$plots$training_vs_projected_cluster_occupancy
projected$ProjectionFit$plots$separation_map
}
}
