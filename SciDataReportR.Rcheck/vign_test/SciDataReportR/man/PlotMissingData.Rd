% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PlotMissingData.R
\name{PlotMissingData}
\alias{PlotMissingData}
\title{Plot Missing Data}
\usage{
PlotMissingData(
  data,
  variables = NULL,
  HoverVars = NULL,
  x_var = NULL,
  facet_by = NULL,
  Relabel = TRUE,
  show_perc = TRUE,
  show_perc_var = TRUE,
  cluster = FALSE,
  DataFrame = lifecycle::deprecated(),
  Variables = lifecycle::deprecated()
)
}
\arguments{
\item{data}{A data frame.}

\item{variables}{Character vector of variables to visualize. If NULL,
all columns except \code{HoverVars}, \code{x_var}, and \code{facet_by} are used.}

\item{HoverVars}{Optional character vector of columns to include in hover
text. Useful for participant IDs, visit names, dates, sites, etc.}

\item{x_var}{Optional single column name to use for the x-axis. Numeric and
date variables retain their original scale; categorical variables use a
discrete axis. Missing x values are displayed as \code{"Missing"}.}

\item{facet_by}{Optional single column name used to create missingness
panels. Missing facet values are displayed in a \code{"Missing"} panel.}

\item{Relabel}{Logical. If TRUE, variable labels are used when available.}

\item{show_perc}{Logical. If TRUE, overall missingness percentages are shown
in the legend.}

\item{show_perc_var}{Logical. If TRUE, variable-specific missingness
percentages are appended to y-axis labels.}

\item{cluster}{Logical. If TRUE, variables are clustered by missingness
pattern.}

\item{DataFrame}{\strong{Deprecated} (since 19.15.0). Use \code{data} instead.}

\item{Variables}{\strong{Deprecated} (since 19.15.0). Use \code{variables} instead.}
}
\value{
A ggplot object.
}
\description{
Visualize missing data patterns with variables as rows and observations as
columns. Optional hover variables can be included to facilitate quality
control workflows when converting the plot to an interactive Plotly figure.
}
\examples{
data(SampleData)
data(SampleVariableTypes)

Labelled <- RevalueData(SampleData, SampleVariableTypes)$RevaluedData

# The revalued data includes missingness defined in the codebook
vars <- c("age", "AXL", "Angiotensinogen", "BMP_6", "IL_6",
          "Fetuin_A", "NT_proBNP", "ENA_78")

PlotMissingData(
  Labelled,
  variables = vars,
  HoverVars = "Diagnosis"
)

}
