% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ClusteringPipelines.R
\name{CreateMclustClusterModel}
\alias{CreateMclustClusterModel}
\title{Fit a projectable Gaussian-mixture clustering model}
\usage{
CreateMclustClusterModel(
  data,
  variables = NULL,
  method = c("exploratory", "finalize"),
  k_range = 2:10,
  model_names = c("EEI", "VVI", "EEE"),
  final_k = NULL,
  final_model = NULL,
  Scaling = c("Center and Scale", "Center Only", "Scale Only", "None", "PreZScored"),
  ClusterName = "Cluster",
  seed = 93421L,
  stability_resamples = 0L,
  stability_seed = seed + 1L,
  stability_progress = FALSE
)
}
\arguments{
\item{data}{Data frame containing numeric clustering variables.}

\item{variables}{Variables used for clustering.}

\item{method}{Either \code{"exploratory"} or \code{"finalize"}.}

\item{k_range}{Candidate cluster counts in exploratory mode.}

\item{model_names}{Mclust covariance model names.}

\item{final_k, final_model}{Finalized cluster count and covariance model.}

\item{Scaling}{Frozen preprocessing: \code{"Center and Scale"}, \code{"Center Only"},
\code{"Scale Only"}, \code{"None"}, or \code{"PreZScored"}.}

\item{ClusterName}{Output cluster column name.}

\item{seed}{Random seed retained for reproducibility.}

\item{stability_resamples}{Number of bootstrap refits used to estimate
candidate reproducibility. Use \code{0} to disable stability analysis.}

\item{stability_seed}{Seed controlling bootstrap resampling.}

\item{stability_progress}{Whether to print bootstrap progress messages.}
}
\value{
A projectable mixture model. \code{ModelInfo$fit_table} contains BIC,
ICL, entropy, uncertainty, and bootstrap stability metrics, and
\code{ModelInfo$AHP} the advisory recommendation. Figures sit beside what they
describe: \code{fit_plot} reviews candidates; \code{ModelInfo$plots} holds \code{bic},
\code{icl}, \code{entropy}, \code{centre_heatmap}, \code{centre_profile}, \code{separation_map},
\code{classification_uncertainty}, and \code{profiles};
\code{ModelInfo$FitDiagnostics$plots} holds the Mahalanobis distance triad;
\code{ProbFit$plots} holds posterior-confidence figures; and \code{Stability$plots}
holds bootstrap agreement and per-cluster recovery.
}
\description{
Best for continuous measures when clinically meaningful groups
may differ in means, variance, or covariance and posterior uncertainty is useful.
}
\examples{
\donttest{
data("SimulatedPhenotypeData")
df_Training <- dplyr::filter(SimulatedPhenotypeData, .data$Cohort == "Training")
df_Projection <- dplyr::filter(SimulatedPhenotypeData, .data$Cohort == "Projection")
vars_Numeric <- paste0("Var", 1:12)
review <- CreateMclustClusterModel(
  df_Training, vars_Numeric, k_range = 2:4, model_names = "EEI",
  stability_resamples = 2
)
review$ModelInfo$fit_table
review$ModelInfo$AHP$recommendation
review$fit_plot
review$ModelInfo$plots$bic
review$Stability$plots$bootstrap_agreement
model <- CreateMclustClusterModel(
  df_Training, vars_Numeric, method = "finalize", final_k = 4,
  final_model = "EEI", stability_resamples = 2
)
model$ModelInfo$plots$centre_heatmap
model$ModelInfo$plots$separation_map
model$ModelInfo$FitDiagnostics$plots$distance_by_cluster_box
model$ProbFit$plots$confidence_density
projected <- ProjectMclustCluster(model, df_Projection)
projected$ProjectionFit$plots$projection_fit_class_bar
}
}
\references{
Scrucca L, Fop M, Murphy TB, Raftery AE. mclust 5: Clustering,
classification and density estimation using Gaussian finite mixture models.
\emph{J Stat Softw.} 2016;8(1):1-29.
}
