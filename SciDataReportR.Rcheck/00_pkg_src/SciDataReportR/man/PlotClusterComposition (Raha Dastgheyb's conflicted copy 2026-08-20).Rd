% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ClusterPlots.R
\name{PlotClusterComposition}
\alias{PlotClusterComposition}
\title{Plot categorical composition by cluster}
\usage{
PlotClusterComposition(
  data,
  variables,
  cluster,
  facet_by = c("variable", "cluster"),
  style = c("stacked", "enrichment")
)
}
\arguments{
\item{data}{Data frame containing the categorical variables.}

\item{variables}{Categorical variable names.}

\item{cluster}{Cluster assignment vector aligned to \code{data}.}

\item{facet_by}{Whether stacked-bar facets represent categorical variables
(the default) or clusters.}

\item{style}{Either \code{"stacked"} for composition bars or \code{"enrichment"} for
a cluster-first heatmap of percentage-point differences from the cohort.}
}
\value{
A \code{ggplot} object, or \code{NULL} when no categorical variables are given.
}
\description{
Plot categorical composition by cluster
}
\examples{
\donttest{
data(SimulatedPhenotypeData)
df_Training <- dplyr::filter(SimulatedPhenotypeData, .data$Cohort == "Training")
model <- CreateClusterModel_Gower_PAM(
  df_Training, c("Var1", "Var2", "CatVar1", "CatVar2"),
  method = "finalize", final_k = 4
)
model$ModelInfo$plots$categorical_composition
}
}
