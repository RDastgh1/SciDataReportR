% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ScreenBiomarkerPerformance.R
\name{ScreenBiomarkerPerformance}
\alias{ScreenBiomarkerPerformance}
\title{Screen biomarker performance across outcomes}
\usage{
ScreenBiomarkerPerformance(
  data,
  outcome_vars,
  biomarker_vars,
  covariates = NULL,
  PositiveLevel = NULL,
  OutcomeType = c("auto", "binary", "continuous"),
  Validation = c("none", "bootstrap", "cross_validation"),
  BootstrapR = 500,
  CVFolds = 10,
  CIBootstrapR = 200,
  CILevel = 0.95,
  HeatmapMetric = "AdjustedAUC",
  Seed = 123,
  Relabel = TRUE,
  codebook = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{outcome_vars}{Character vector of outcome variable names.}

\item{biomarker_vars}{Character vector of biomarker variable names.}

\item{covariates}{Optional character vector of covariate variable names.}

\item{PositiveLevel}{Optional positive level. Supply one value for all binary
outcomes or a named character vector with names matching \code{outcome_vars}.}

\item{OutcomeType}{One of \code{"auto"}, \code{"binary"}, or \code{"continuous"}, applied
to all outcomes unless auto-detection is used.}

\item{Validation}{One of \code{"none"}, \code{"bootstrap"}, or
\code{"cross_validation"}.}

\item{BootstrapR}{Number of bootstrap resamples for internal validation.}

\item{CVFolds}{Number of cross-validation folds.}

\item{CIBootstrapR}{Number of bootstrap resamples for performance confidence
intervals.}

\item{CILevel}{Confidence level. Default is \code{0.95}.}

\item{HeatmapMetric}{Performance metric shown by the heatmap fill. Default is
\code{"AdjustedAUC"}. Common alternatives are \code{"AUC"}, \code{"DeltaAUC"},
\code{"AdjustedR2"}, and \code{"DeltaR2"}.}

\item{Seed}{Random seed used for resampling.}

\item{Relabel}{Logical indicating whether labels should be used when available.}

\item{codebook}{Optional data frame with columns \code{Variable} and \code{Label}.}
}
\value{
A named list with \code{PerformanceTable}, \code{RegressionTable},
\code{ThresholdTable}, \code{FailureTable}, \code{Evaluations}, \code{Plots}, and \code{Metadata}.
For binary outcomes, \code{Plots} also includes \code{BiomarkerPanels} (raw
outcome-stratified distributions for continuous biomarkers) and \code{ROCFacets}
(adjusted ROC curves), each annotated with pair-specific performance.
}
\description{
Applies \code{\link[=EvaluateBiomarkerPerformance]{EvaluateBiomarkerPerformance()}} across many candidate biomarkers and
one or more binary or continuous outcomes. Each biomarker-outcome pair uses
all complete observations available for that pair and the requested
covariates. The function returns comparison tables and screening plots,
including an interactive-ready heatmap whose cells contain hover text.
}
\examples{
\dontrun{
screen <- ScreenBiomarkerPerformance(
  data = df,
  outcome_vars = c("DiseaseCohort", "Progression"),
  biomarker_vars = c("NfL", "GFAP", "Acrocyanosis"),
  covariates = c("Age", "Sex")
)

screen$PerformanceTable
screen$Plots$Heatmap
screen$Plots$Heatmap \%>\% add_biomarker_values()
}
}
