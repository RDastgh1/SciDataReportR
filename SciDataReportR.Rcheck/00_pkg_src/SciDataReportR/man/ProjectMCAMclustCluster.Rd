% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CategoricalClusteringPipelines.R
\name{ProjectMCAMclustCluster}
\alias{ProjectMCAMclustCluster}
\title{Project data onto an MCA + Mclust model}
\usage{
ProjectMCAMclustCluster(object, new_df, ClusterName = object$ClusterName)
}
\arguments{
\item{object}{A \code{CreateMclustClusterModel()} result.}

\item{new_df}{New observations.}

\item{ClusterName}{Optional output cluster column name.}
}
\value{
A full-length projection through the frozen MCA and Mclust layers,
with posterior membership in \code{ProbFit} and the projection triage in
\code{ProjectionFit}. Incomplete categorical rows are retained with \code{NA}
assignments.
}
\description{
Project data onto an MCA + Mclust model
}
\examples{
\donttest{
data("SimulatedPhenotypeData")
df_Training <- dplyr::filter(SimulatedPhenotypeData, .data$Cohort == "Training")
df_Projection <- dplyr::filter(SimulatedPhenotypeData, .data$Cohort == "Projection")
model <- CreateMCAMclustClusterModel(
  df_Training, paste0("CatVar", 1:3), method = "finalize",
  final_k = 4, final_model = "EEI", stability_resamples = 2
)
projected <- ProjectMCAMclustCluster(model, df_Projection)
head(projected$ProbFit$individual)
projected$ProjectionFit$plots$projection_fit_class_bar
projected$ProjectionFit$plots$training_vs_projected_cluster_occupancy
}
}
