% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/InspectFile.R
\name{InspectFile}
\alias{InspectFile}
\title{Inspect a scientific data file before import}
\usage{
InspectFile(
  path,
  sheet = NULL,
  preview_rows = 20,
  check_styles = TRUE,
  check_sheets = TRUE,
  check_header = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{path}{Path to the file.}

\item{sheet}{Sheet name or index for Excel files. If \code{NULL}, the first sheet
is inspected.}

\item{preview_rows}{Number of rows to preview when checking header and column
name issues.}

\item{check_styles}{Logical. For \code{.xlsx} files, check whether workbook styles
or formatting exist. This can be slower for large workbooks.}

\item{check_sheets}{Logical. For Excel files, check whether the workbook has
multiple sheets.}

\item{check_header}{Logical. For Excel files, attempt to detect whether the
header row is not row 1.}

\item{quiet}{Logical. If \code{FALSE}, print a compact inspection summary.}
}
\value{
Invisibly returns a list containing file inspection metadata.
}
\description{
\code{InspectFile()} checks common import risks before a file is read into an
analysis workflow. It is especially useful for Excel workbooks, where
multiple sheets, metadata rows, unnamed columns, duplicate column names, and
workbook formatting can change how the data should be interpreted.
}
\details{
This function does not modify the file or the imported data. It only reports
what it finds.
}
