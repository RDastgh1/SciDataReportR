% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ClusteringPipelines.R
\name{CreateHDBSCANClusterModel}
\alias{CreateHDBSCANClusterModel}
\title{Fit a projectable HDBSCAN model}
\usage{
CreateHDBSCANClusterModel(
  data,
  variables = NULL,
  method = c("exploratory", "finalize"),
  minPts_range = 2:10,
  cluster_selection_epsilon_range = c(0, 0.05, 0.1),
  final_minPts = NULL,
  final_cluster_selection_epsilon = NULL,
  minPts = NULL,
  Scaling = c("Center and Scale", "Center Only", "Scale Only", "None", "PreZScored"),
  ClusterName = "Cluster",
  seed = 93421L,
  stability_resamples = 0L,
  stability_seed = seed + 1L,
  stability_progress = FALSE
)
}
\arguments{
\item{data}{Data frame containing numeric clustering variables.}

\item{variables}{Variables used for clustering.}

\item{method}{Either \code{"exploratory"} or \code{"finalize"}.}

\item{minPts_range}{Candidate minimum-points settings in exploratory mode.}

\item{cluster_selection_epsilon_range}{Candidate extraction epsilon values.}

\item{final_minPts, final_cluster_selection_epsilon}{Finalized density settings.}

\item{minPts}{Deprecated-compatible shortcut for a finalized \code{minPts} fit.}

\item{Scaling}{Frozen preprocessing: \code{"Center and Scale"}, \code{"Center Only"},
\code{"Scale Only"}, \code{"None"}, or \code{"PreZScored"}.}

\item{ClusterName}{Output cluster column name.}

\item{seed}{Random seed retained for reproducibility.}

\item{stability_resamples}{Number of bootstrap refits used to estimate
candidate reproducibility. Use \code{0} to disable stability analysis.}

\item{stability_seed}{Seed controlling bootstrap resampling.}

\item{stability_progress}{Whether to print bootstrap progress messages.}
}
\value{
A fitted HDBSCAN model with cluster/noise assignments, membership
probabilities, outlier scores, frozen nearest-core support thresholds, and
bootstrap ARI/Jaccard and noise-recovery metrics. Figures sit beside what
they describe: \code{fit_plot} reviews the density grid;
\code{ModelInfo$plots} holds \code{density_review}, \code{persistence},
\code{cluster_persistence}, \code{separation_map}, and \code{profiles}; \code{ModelInfo$FitDiagnostics$plots} holds the
core-distance triad and \code{outlier_score_by_cluster}; \code{ProbFit$plots} holds
membership-probability figures; and \code{Stability$plots} holds bootstrap
agreement, per-cluster recovery, and noise recovery.
}
\description{
Best for irregularly shaped numeric clusters, variable density,
and data where a meaningful noise/outlier group is expected.
}
\examples{
\donttest{
data("SimulatedPhenotypeData")
df_Training <- dplyr::filter(SimulatedPhenotypeData, .data$Cohort == "Training")
df_Projection <- dplyr::filter(SimulatedPhenotypeData, .data$Cohort == "Projection")
review <- CreateHDBSCANClusterModel(
  df_Training, c("DensityX", "DensityY"), minPts_range = c(6, 10),
  cluster_selection_epsilon_range = c(0, 0.05),
  stability_resamples = 2
)
review$ModelInfo$fit_table
review$ModelInfo$AHP$recommendation
review$fit_plot
review$ModelInfo$plots$density_review
model <- CreateHDBSCANClusterModel(
  df_Training, c("DensityX", "DensityY"), method = "finalize",
  final_minPts = 10, final_cluster_selection_epsilon = 0,
  stability_resamples = 2
)
model$ModelInfo$plots$separation_map
model$ModelInfo$plots$cluster_persistence
model$ModelInfo$FitDiagnostics$plots$outlier_score_by_cluster
projected <- ProjectHDBSCANCluster(model, df_Projection)
projected$ProjectionFit$plots$nearest_core_support
}
}
\references{
McInnes L, Healy J, Astels S. hdbscan: Hierarchical density based
clustering. \emph{J Open Source Softw.} 2017;2(11):205.
}
