% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CategoricalClusteringPipelines.R
\name{CreateClusterModel_MCA_MClust}
\alias{CreateClusterModel_MCA_MClust}
\title{Fit MCA followed by Mclust for nominal categorical data}
\usage{
CreateClusterModel_MCA_MClust(
  data,
  variables,
  method = c("exploratory", "finalize"),
  k_range = 2:10,
  models = c(1L, 2L, 3L),
  final_k = NULL,
  final_model = NULL,
  ClusterVariableName = "Cluster",
  seed = 93421L,
  mca_variance_threshold = 75,
  stability_resamples = 0L,
  stability_seed = seed + 1L,
  stability_progress = FALSE
)
}
\arguments{
\item{data}{Data frame containing numeric clustering variables.}

\item{variables}{Variables used for clustering.}

\item{method}{Either \code{"exploratory"} or \code{"finalize"}.}

\item{k_range}{Candidate cluster counts in exploratory mode.}

\item{models}{Numeric Mclust model IDs: \code{1} = EEI, \code{2} = VVI, \code{3} = EEE.}

\item{final_k, final_model}{Finalized cluster count and numeric model ID.}

\item{ClusterVariableName}{Output cluster column name.}

\item{seed}{Random seed retained for reproducibility.}

\item{mca_variance_threshold}{Cumulative MCA inertia percentage retained.}

\item{stability_resamples}{Number of bootstrap refits used to estimate
candidate reproducibility. Use \code{0} to disable stability analysis.}

\item{stability_seed}{Seed controlling bootstrap resampling.}

\item{stability_progress}{Whether to print bootstrap progress messages.}
}
\value{
A fitted MCA plus Mclust model containing the frozen MCA object,
mixture-model fit and bootstrap stability metrics, and posterior
probabilities. \code{ModelInfo$plots} leads with the reduction layer's \code{scree}
and \code{loadings}, followed by the mixture-model structure figures in MCA
score space and \code{categorical_composition} restated on the original items.
BIC/ICL/AIC, entropy, and uncertainty use the same interpretation as Mclust.
}
\description{
Use this pipeline for many nominal categorical variables when a
lower-dimensional category space is useful before mixture clustering.
}
\examples{
\donttest{
data("SimulatedPhenotypeData")
df_Training <- dplyr::filter(SimulatedPhenotypeData, .data$Cohort == "Training")
df_Projection <- dplyr::filter(SimulatedPhenotypeData, .data$Cohort == "Projection")
vars_Categorical <- paste0("CatVar", 1:3)
review <- CreateClusterModel_MCA_MClust(
  df_Training, vars_Categorical, k_range = 2:4, models = 1,
  stability_resamples = 2
)
review$ModelInfo$fit_table
review$ModelInfo$AHP$recommendation
review$fit_plot
review$ModelInfo$plots$scree
model <- CreateClusterModel_MCA_MClust(
  df_Training, vars_Categorical, method = "finalize", final_k = 4,
  final_model = 1, stability_resamples = 2
)
model$ModelInfo$plots$separation_map
model$ModelInfo$plots$categorical_composition
projected <- ProjectCluster(model, df_Projection)
projected$ProjectionFit$plots$projection_fit_class_bar
}
}
\references{
Greenacre M. \emph{Correspondence Analysis in Practice}. 3rd ed.
Chapman and Hall/CRC; 2017. Scrucca L et al. mclust 5. \emph{J Stat Softw.} 2016;71(11):1-29.
}
