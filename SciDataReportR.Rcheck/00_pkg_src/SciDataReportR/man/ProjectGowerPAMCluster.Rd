% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ClusteringPipelines.R
\name{ProjectGowerPAMCluster}
\alias{ProjectGowerPAMCluster}
\title{Project data onto a Gower + PAM model}
\usage{
ProjectGowerPAMCluster(object, new_df, ClusterName = object$ClusterName)
}
\arguments{
\item{object}{A \code{CreateMclustClusterModel()} result.}

\item{new_df}{New observations.}

\item{ClusterName}{Optional output cluster column name.}
}
\value{
A full-length projection using frozen Gower ranges, levels, and
medoids. \code{ProbFit} holds the distance to the assigned medoid and the
margin to the second-nearest medoid; \code{ProjectionFit} triages every case
against the frozen training distance reference and holds the projected
distance triad, a \code{medoid_map} placing cases by their two nearest medoid
distances, the training-versus-projected occupancy comparison, and the
fit-class bar.
}
\description{
Project data onto a Gower + PAM model
}
\examples{
\donttest{
data("SimulatedPhenotypeData")
df_Training <- dplyr::filter(SimulatedPhenotypeData, .data$Cohort == "Training")
df_Projection <- dplyr::filter(SimulatedPhenotypeData, .data$Cohort == "Projection")
vars_Mixed <- c("Var1", "Var2", "Var3", "CatVar1", "CatVar2", "CatVar3")
model <- CreateGowerPAMClusterModel(
  df_Training, vars_Mixed, method = "finalize", final_k = 4,
  stability_resamples = 2
)
projected <- ProjectGowerPAMCluster(model, df_Projection)
head(projected$ProbFit$individual)
projected$ProjectionFit$plots$medoid_map
projected$ProjectionFit$plots$training_vs_projected_cluster_occupancy
}
}
